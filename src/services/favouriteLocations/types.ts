export type FavouriteLocation = {
  id: string;
  city: string;
  country: string;
  latitude: number;
  longitude: number;
  created: string;
  updated: string;
  collectionId: string;
  collectionName: string;
  expand: Record<string, never>;
};
